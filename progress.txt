## Codebase Patterns
- El contenido de la definición (RAE/Wikcionario) SIEMPRE sale tal cual de la fuente real -- Gemini solo se usa en src/llm/gemini_client.py para extraer qué palabra pidió el usuario, nunca para redactar/reescribir la definición.
- Frases de personalidad (saludos, "trabajando", notas de fe) viven en src/bot/texts.py como listas rotativas (random.choice) + una función helper por lista. No mezclar este texto con el contenido real de la definición en formatters.py.
- formatters.py arma el mensaje final; siempre termina con texts.toque_de_fe() (US-001) -- si se agregan nuevas secciones (sinónimos, etimología, etc.) deben ir ANTES de esa línea final.
- Flujo de handlers.py: 1) extraer_heuristico() sin IA -> 2) si falla, Gemini SOLO para extraer el concepto -> 3) enviar texts.trabajando() como primer mensaje -> 4) consultar RAE + Wikcionario -> 5) formatear y enviar el resultado como segundo mensaje.
- Wikcionario: usar [ \t]* (no \s*) entre el número de definición y su etiqueta en la regex de src/services/wikcionario.py -- \s* también matchea saltos de línea y rompe el parseo cuando no hay etiqueta en la misma línea.
- concept_extractor.py: cualquier prefijo de pregunta debe permitir opcionalmente un artículo (_ARTICULO = el/la/los/las/un/una/unos/unas) antes de la palabra, si no el heurístico devuelve "la palabra" en vez de "palabra".
- Reinicio del proceso en vivo: usar `pgrep -f "conceptos-bot.*run.py" -a` para encontrar el PID hijo real de python (no el wrapper de bash), matar con SIGTERM, esperar unos segundos, confirmar con pgrep que quedó en cero antes de arrancar una instancia nueva. Si SIGTERM no limpia el proceso en pocos segundos, usar SIGKILL.
- Tests: pytest con mocks de urllib.request.urlopen para los clientes de servicios (sin red real). Correr con `.venv/bin/python -m pytest -q` desde la raíz del proyecto.
- rae-api.com trae más datos de los que se muestran por defecto: cada "sense" tiene examples (list[str]) y synonyms (list[str]); cada "meaning" tiene origin.raw (etimología). src/services/rae.py ya los expone en el dict de retorno (etimologia a nivel raíz, ejemplos/sinonimos por acepción) -- usar .get(...) con default [] al leerlos, porque no siempre están presentes.

## [2026-07-03 23:36] - US-001
- Implementado el "toque de fe" al final de cada respuesta de concepto y el flujo de dos mensajes ("trabajando" antes de consultar RAE/Wikcionario, resultado formateado después).
- Archivos cambiados:
  - src/bot/texts.py: agregadas listas TRABAJANDO y TOQUE_DE_FE (rotativas) + funciones trabajando() y toque_de_fe().
  - src/bot/formatters.py: formatear_resultado() ahora agrega texts.toque_de_fe() al final del mensaje.
  - src/bot/handlers.py: handle_message() envía texts.trabajando() como mensaje inmediato apenas se confirma un concepto válido, antes de las consultas de red a RAE/Wikcionario.
  - tests/test_formatters.py: nuevo test test_formatear_incluye_toque_de_fe verificando que el mensaje final contiene una de las notas de fe.
- Verificación: `.venv/bin/python -m pytest -q` -> 19 passed. Proceso en vivo reiniciado (instancia anterior terminada con SIGTERM, cero instancias confirmadas con pgrep, nueva instancia arrancada y verificada en logs sin conflictos de getUpdates).
- **Learnings for future iterations:**
  - El texto de "toque de fe" se concatena directo (partes.strip() + texts.toque_de_fe()) porque la lista ya incluye su propio "\n\n" al inicio -- si se edita TOQUE_DE_FE, mantener ese prefijo.
  - El mensaje de "trabajando" se envía justo después de descartar concepto is None (o sea, ya se sabe que hay una palabra válida que buscar) y antes de los try/except de red -- es el punto correcto para cubrir la latencia real de las llamadas a RAE/Wikcionario.
  - No hace falta un test_texts.py separado por ahora -- test_formatear_incluye_toque_de_fe ya cubre indirectamente que toque_de_fe() funciona y que su salida aparece en el mensaje final.
---

## [2026-07-03 23:45] - US-002
- Implementada la extracción y el despliegue de etimología, ejemplos de uso y sinónimos que rae-api.com ya entrega pero que antes se descartaban.
- Archivos cambiados:
  - src/services/rae.py: buscar_rae() ahora devuelve "etimologia" (str|None, tomada de meanings[0].origin.raw) a nivel raíz, y cada acepción incluye "ejemplos" (list[str]) y "sinonimos" (list[str]), tomados de sense["examples"]/sense["synonyms"].
  - src/bot/formatters.py: _lista_acepciones() ahora agrega sub-líneas con 💬 (ejemplo) y 🔗 (hasta 5 sinónimos) debajo de cada acepción cuando existen; formatear_resultado() agrega una línea "_Origen: ..._" con la etimología si está presente. Todo opcional -- no rompe cuando faltan estos campos (Wikcionario no los tiene).
  - tests/test_rae.py: fixture RESPUESTA_CASA ahora incluye origin/examples/synonyms; nuevo test test_buscar_rae_incluye_etimologia_ejemplos_y_sinonimos.
  - tests/test_formatters.py: nueva fixture RAE_CASA_ENRIQUECIDA y tests test_formatear_incluye_etimologia_ejemplos_y_sinonimos / test_formatear_sin_etimologia_ni_ejemplos_no_falla.
- Verificación: `.venv/bin/python -m pytest -q` -> 22 passed. Probado también en vivo contra rae-api.com real (palabra "casa") para confirmar que el mensaje se ve bien formateado en Markdown.
- **Learnings for future iterations:**
  - Wikcionario (_lista_acepciones también la usa para sus definiciones) nunca tendrá "ejemplos"/"sinonimos" -- por eso _lista_acepciones usa item.get(...) or [] en vez de asumir que existen esas claves.
  - El límite de sinónimos mostrados es _MAX_SINONIMOS_MOSTRADOS = 5 en formatters.py, para no saturar el mensaje cuando la RAE trae listas largas (ej. "casa" trae 12 sinónimos en su primera acepción).
---

## [2026-07-03 23:50] - US-003
- Implementado el comando /menu con teclado inline, siguiendo el patrón menu_command/menu_callback de gastos-bot (Coco).
- Archivos cambiados:
  - src/bot/texts.py: nuevo texto ACERCA_DE (blurb de Francis, con su toque cristocéntrico).
  - src/bot/commands.py: nueva _menu_keyboard() (InlineKeyboardMarkup), menu_command() y menu_callback() (namespace de callback_data "francis_menu:...").
  - src/main.py: registrado CommandHandler("menu", menu_command), CallbackQueryHandler(menu_callback, pattern=r"^francis_menu:"), y "menu" agregado a COMANDOS_PUBLICOS (BotCommand) para que aparezca en el menú ☰ de Telegram.
  - tests/test_commands.py (nuevo): tests con AsyncMock/MagicMock para menu_command y menu_callback.
  - requirements-dev.txt: agregado pytest-asyncio==0.24.0 (necesario para @pytest.mark.asyncio; antes no había tests async en este proyecto).
- Verificación: `.venv/bin/python -m pytest -q` -> 25 passed. `python -c "import src.main"` para confirmar que los nuevos imports/registro de handlers no rompen el arranque.
- **Learnings for future iterations:**
  - El menú por ahora solo tiene botones "Cómo consultarme" (ayuda) y "Acerca de Francis" -- NO se agregaron botones de favoritos/historial porque esas features (US-005/US-006) todavía no existen. Cuando se implementen, agregar sus botones a _menu_keyboard() en src/bot/commands.py y sus ramas correspondientes en menu_callback().
  - Patrón de callback_data: "francis_menu:<accion>" (namespace por bot, igual que "coco_menu:" en gastos-bot), para no chocar con otros CallbackQueryHandler que se agreguen más adelante (ej. futuros botones de favoritos tendrían su propio prefijo, como hace gastos-bot con coco_saldo/coco_cat/etc.).
  - Para testear handlers async de telegram sin conexión real: MagicMock() para Update/Context + AsyncMock() en los métodos que se awaitan (reply_text, answer, etc.), y @pytest.mark.asyncio en el test. Requiere pytest-asyncio instalado (ya en requirements-dev.txt).
---

## [2026-07-03 23:55] - US-004
- Implementado el modo inline: se puede escribir "@francislabuhobibliotecaria_bot palabra" desde cualquier chat de Telegram y Francis responde con un resultado inline (RAE + Wikcionario).
- Archivos cambiados:
  - src/bot/handlers.py: extraída _buscar_concepto(concepto) (helper compartido que antes estaba inline en handle_message, ahora reutilizado) y agregado inline_query_handler(update, context) que usa InlineQueryResultArticle + InputTextMessageContent.
  - src/main.py: registrado InlineQueryHandler(inline_query_handler).
  - tests/test_handlers.py (nuevo): tests con mocks de buscar_rae/buscar_wikcionario para query vacía, encontrada y no encontrada.
- Verificación: `.venv/bin/python -m pytest -q` -> 28 passed. `python -c "import src.main"` para confirmar el arranque.
- **Learnings for future iterations:**
  - IMPORTANTE -- paso manual pendiente fuera de este código: el modo inline debe habilitarse una vez vía @BotFather con el comando /setinline (no existe endpoint de la Bot API para esto). Sin ese paso, Telegram no enviará las inline queries al bot aunque el InlineQueryHandler ya esté registrado y funcionando en el código.
  - Por velocidad (las inline queries de Telegram tienen un límite de tiempo corto, ~10s), inline_query_handler NO usa Gemini -- solo extraer_heuristico(consulta), y si no matchea ningún patrón, usa la consulta tal cual como concepto. Esto es intencional y diferente del flujo normal de handle_message.
  - _buscar_concepto() en src/bot/handlers.py es ahora el punto único para las llamadas a RAE/Wikcionario con manejo de errores -- si se agrega otra vía de entrada (ej. comando manual, palabra del día), reutilizar esta función en vez de duplicar los try/except.
---

## [2026-07-04 00:05] - US-005 y US-006
- Implementados juntos (comparten la misma base SQLite): favoritos e historial de búsquedas por usuario.
- Archivos cambiados:
  - src/db.py (nuevo): DBClient liviano (sin mixins -- solo dos tablas simples, no se justifica la arquitectura de mixins de gastos-bot/db.py que es para un dominio financiero mucho más grande). Tablas `favoritos` (PK compuesta user_id+palabra) e `historial` (con id autoincrement, usado como desempate de orden en vez de fecha porque datetime('now') de SQLite solo tiene resolución de segundo). Métodos: agregar_favorito, quitar_favorito, es_favorito, listar_favoritos, registrar_historial, listar_historial(limite=10).
  - src/config.py: agregado Config.db_path (env DB_PATH, default "conceptos.db").
  - src/main.py: se instancia DBClient(config.db_path) y se guarda en application.bot_data["db"]; registrados CommandHandler("favoritos", ...), CommandHandler("historial", ...) y CallbackQueryHandler(favorito_callback, pattern=r"^francis_fav:").
  - src/bot/commands.py: favoritos_command, historial_command, favorito_callback (namespace de callback_data "francis_fav:add:<palabra>" / "francis_fav:quitar:<palabra>"), _favoritos_keyboard() con un botón "quitar" por favorito.
  - src/bot/handlers.py: handle_message ahora registra cada búsqueda exitosa en el historial y adjunta un botón "⭐ Guardar en favoritos" al mensaje de resultado (solo si el concepto todavía no es favorito, y solo si cabe en el límite de 40 bytes de callback_data).
  - .gitignore: agregado "*.db" (el archivo SQLite es dato de usuario, no se versiona -- igual que .env).
  - .env.example: agregada variable DB_PATH documentada.
  - tests/test_db.py (nuevo), tests/test_commands.py y tests/test_handlers.py extendidos.
- Verificación: `.venv/bin/python -m pytest -q` -> 44 passed. `python -c "import src.main"` para confirmar el arranque con el DBClient real.
- **Learnings for future iterations:**
  - Para conceptos-bot NO se replicó el patrón de mixins de gastos-bot/db.py -- ese patrón es para ~7 dominios de datos financieros con mucha lógica cada uno. Con dos tablas simples (favoritos, historial), una sola clase plana en src/db.py es más clara y evita abstracción prematura. Si en el futuro se agregan muchas más tablas/dominios, ahí sí valdría la pena revisar si conviene dividir en mixins.
  - `historial` usa un `id INTEGER PRIMARY KEY AUTOINCREMENT` como criterio de orden real (MAX(id) en vez de MAX(fecha)) -- si solo se usara `fecha` (texto con resolución de segundo), inserciones rápidas en el mismo segundo (ej. en tests, o un usuario reconsultando la misma palabra dos veces seguidas) producirían empates y un orden no determinista.
  - Todas las consultas a favoritos/historial normalizan la palabra a minúsculas (palabra.lower()) antes de guardar/leer, para que "Casa" y "casa" cuenten como el mismo favorito.
  - El botón de favorito en el resultado de handle_message se omite si `len(concepto.encode("utf-8")) > 40`, porque el callback_data completo (con el prefijo "francis_fav:add:") no puede superar el límite de 64 bytes que impone la Bot API de Telegram.
---
